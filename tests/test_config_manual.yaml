config:

  - name: Runner repository group
    workspace: test-workspace
    repository: test-repo
    namespace: test-namespace
    labels:
      - test-label
    strategy: manual
    parameters:
      runners_count: 1
